import { NovelSortPipe } from './novel-sort.pipe';

describe('VisualNovelFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NovelSortPipe();
    expect(pipe).toBeTruthy();
  });
});
