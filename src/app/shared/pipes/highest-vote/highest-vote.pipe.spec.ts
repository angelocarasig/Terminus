import { HighestVotePipe } from './highest-vote.pipe';

describe('HighestVotePipe', () => {
  it('create an instance', () => {
    const pipe = new HighestVotePipe();
    expect(pipe).toBeTruthy();
  });
});
